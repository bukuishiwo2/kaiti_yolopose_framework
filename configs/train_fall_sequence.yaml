data:
  dataset: data/processed/urfall_pose_sequences.npz

model:
  hidden_dim: 128
  num_layers: 2
  dropout: 0.2
  bidirectional: false

train:
  device: 0
  batch_size: 64
  epochs: 20
  learning_rate: 0.001
  weight_decay: 0.0001
  threshold: 0.5
  # pos_weight: 1.0

output:
  checkpoint: models/fall_sequence_lstm.pt
